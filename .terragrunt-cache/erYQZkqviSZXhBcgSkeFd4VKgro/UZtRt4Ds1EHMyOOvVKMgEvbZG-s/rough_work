Let's use a root module that references the S3 backend that has been created. So the script creates the S3 backend and then moves to the root module to create the rest. The 120s should give the bucket time for creation. For now let's leave the "prompt to continue" in the script.








PLEASE ADD THE POLICY MENTIONED BELOW:
Flow logs bucket permissions: The bucket policy isn't included. You'll need to attach a policy allowing VPC to write logs. I omitted it for brevity but can add it.


Next Steps
Test this module locally with terragrunt plan from your dev environment.

Add the variables.tf and outputs.tf files (I'll write these next).

Add the security groups module (after VPC is stable).